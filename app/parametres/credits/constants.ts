// Packs de crédits IA proposés (en centimes d'euro).
// Module de constantes neutre (aucune directive serveur) : importable
// par les Server Actions comme par les Server Components.
export const CREDIT_PACKS = [500, 1000, 2500, 5000] as const;
